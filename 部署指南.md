# 基金组合健康度分析 · 云端部署指南

## 这个项目为什么必须带着后端一起部署？
网页 `index.html` 本身只是前端，**真正抓天天基金数据的是 `app.py` 这个后端代理**（浏览器直接抓会被跨域 / Referer 拦掉）。所以部署时要把 `app.py`、`index.html`、`static/` 整个文件夹一起传上去，不能只传静态文件。

本项目**零第三方依赖**（只用 Python 标准库），并且端口已改为读环境变量 `PORT`，所以放到任何支持 Python 的云平台都是「上传 → 启动 `python app.py`」两步走。

---

## 方式一：Render（最简单，GitHub 一键，免费）
适合：不想装命令行、希望点几下就上线的人。

1. 注册 https://render.com （可用 GitHub 账号直接登录）。
2. 把本项目推到你自己的 GitHub 仓库（下面命令在你电脑的 fund-analyzer 目录下执行）：
   ```bash
   git remote add origin https://github.com/<你的用户名>/fund-analyzer.git
   git branch -M main
   git push -u origin main
   ```
3. Render 控制台 → **New** → **Web Service** → 关联上面这个仓库。
4. 关键设置（仓库里已带 `render.yaml`，大部分会自动填好）：
   - Runtime：**Python 3**
   - Build Command：留空（无需安装依赖）
   - Start Command：`python app.py`
5. 免费版会在约 15 分钟无访问后**休眠**，下次打开要等 ~30 秒唤醒（个人看盘完全够用）。
6. 部署完得到 `https://fund-analyzer-xxxx.onrender.com`，手机 / 任意设备浏览器打开即用。

---

## 方式二：Railway（免 GitHub，CLI 直传，有免费额度）
适合：不想建 GitHub 仓库、想从本地文件夹直接推的人。

1. 注册 https://railway.app 。
2. 在你电脑上装 CLI：`brew install railway`（或 `npm i -g @railway/cli`）。
3. 进入 fund-analyzer 目录执行：
   ```bash
   railway login     # 浏览器授权
   railway init      # 新建项目
   railway up        # 上传并部署
   ```
4. 部署后执行 `railway domain` 拿到公网网址。
   仓库里的 `railway.json` 已配置好用 `python app.py` 启动并做了健康检查。

---

## 方式三：腾讯云（国内主机，抓天天基金最稳，需腾讯云账号）
适合：希望长期稳定、且数据延迟最低的人（服务器在国内，访问天天基金最快最稳）。

- 用**云函数 SCF → Web 函数**（推荐，按量计费几乎免费）：
  - 运行环境选 **Python 3.9**
  - 上传整个项目文件夹（含 `app.py` / `index.html` / `static/` / **`scf_bootstrap`**）
  - 绑定 API 网关或默认域名后即可公网访问
- 注意：需要腾讯云账号，配置步骤比 Render 多一点，但最稳、不会因海外节点抽风而拿不到数据。
- 👉 **详细分步教程见同目录 `腾讯云SCF部署指南.md`**（已含专用启动文件 + zip 命令 + 备选轻量服务器方案）。

---

## 通用注意事项
- 数据来自天天基金公开接口，仅用于个人分析，请勿高频请求。
- 改了代码之后：Render / Railway 重新推送即自动重新部署；腾讯云重新上传函数代码即可。
- 如果某次打不开或没数据，先看平台日志里后端能否访问 `eastmoney.com` 系列域名；个别海外节点偶尔不稳，**国内主机（方式三）最可靠**。
- 本机仍可用：`cd fund-analyzer && python3 app.py`，访问 http://localhost:8000 。
