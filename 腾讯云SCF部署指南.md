# 基金组合健康度分析 · 腾讯云 SCF Web 函数部署指南

本软件已在项目里放好腾讯云专用启动文件 `scf_bootstrap`，**零第三方依赖**，按下面步骤即可得到一个常驻公网的网址，手机 / 任意设备打开即用。服务器在国内，抓天天基金数据延迟最低、最稳。

---

## 前置条件
- 一个**已完成实名认证**的腾讯云账号（国内云必须实名，否则无法建函数）。
- 本机 Mac 上已装好 `zip`（系统自带）。

---

## 第一步：在 SCF 控制台建 Web 函数
**入口**：腾讯云控制台顶部搜「云函数」，或直接打开 https://console.cloud.tencent.com/scf/list
（产品全称是「云函数 SCF」，**不要**进「CloudBase 云开发」或「Serverless 应用」，那不是同一个东西。）

1. 左侧菜单点 **「函数服务」**，页面右上角点 **「新建」**。
2. **关键一步**：新建函数页面顶部有两个大卡片 / 标签 —— **「事件函数」** 和 **「Web 函数」**。
   默认选中「事件函数」，**请点「Web 函数」这个卡片**（很多人卡在这，看到的是事件函数向导、找不到 Web 选项）。
3. 创建方式选「从头开始」（或空白函数）。
4. 地域：选离你近的（如「广州」「上海」）。
5. 函数名称：`fund-analyzer`（随意）。
6. 运行环境：**Python 3.9**（若要 3.10 见下方注意）。
7. 其余默认，点「完成 / 下一步」先建一个空函数（稍后传代码）。

---

## 第二步：打包代码
在项目目录（含 `app.py` / `index.html` / `static/` / `scf_bootstrap`）执行：

```bash
cd fund-analyzer
zip -r ../fund-analyzer.zip . -x "*.DS_Store" "__pycache__/*"
```

> 说明：`scf_bootstrap` 已设为 755 可执行权限，命令行 `zip` 会保留该权限。
> 如果你改用「访达右键压缩」或某些 GUI 工具导致权限丢失，部署后访问报 **405**，进控制台代码编辑器把 `scf_bootstrap` 内容原样贴一遍即可（内容见文末）。

---

## 第三步：上传部署
1. 在函数详情 → **函数代码** → 上传方式选「**本地打包上传**」，选中 `fund-analyzer.zip`。
2. 点「**部署 / 保存**」。
3. 等几秒，状态变「**部署成功**」。

> 若你选「控制台快速创建」而非本地上传：因项目里已有 `scf_bootstrap`，系统会以项目里的为准；保险起见部署后仍去代码编辑器确认 `scf_bootstrap` 第一行是 `#!/bin/bash` 且内容如上。

---

## 第四步：开启「函数 URL」拿到公网网址
> ⚠️ **重要变更**：腾讯云 **API 网关触发器已于 2025-06-30 正式下线，不再支持新建**（存量触发器也已不可用）。Web 函数改为用「**函数 URL**」获取公网地址，步骤更简单。

1. 函数详情页，**左侧导航点「函数 URL」**（注意：不是「触发管理」里的 API 网关，那条路已封）。
2. 点 **「新建函数 URL」**。
3. 配置：
   - 别名/版本：默认 `$LATEST` 即可；
   - 访问：勾选 **公网访问**（EnableExtranet）；
   - 授权类型：选 **「开放」**（即匿名 NONE，任何人可访问，适合你的看盘面板）。
4. 点「确定」，页面生成网址，形如：
   ```
   https://xxxx.scm.tencentcs.com/   （以函数URL页实际显示为准）
   ```
5. 复制到浏览器打开 —— 看到组合分析仪表盘即成功。
6. 顺手验证接口：`网址/api/fund/110011`，应返回一串 JSON（含基金名称、净值等）。

---

## 第五步（可选）：自定义域名 + HTTPS
- 在 **API 网关控制台** 给这个函数绑定你自己的域名并开启 HTTPS（需你有备案域名）。
- 不绑也行，默认的 `apigw.tencentcs.com` 地址已支持 HTTPS，直接手机用没问题。

---

## 费用
- Web 函数**按量计费**，有免费额度（调用次数 + 资源使用量），个人低频看盘**几乎白嫖**。
- 唯一可能产生小额费用的是**公网出流量**（返回给浏览器的数据量），看盘量级可忽略。
- 比「轻量应用服务器」更省钱，且不用管系统维护。

---

## 日常维护
- **改了代码**：重新 `zip` 上传覆盖，或直接在控制台代码编辑器在线改 `app.py` / `index.html`。
- **冷启动**：函数一段时间无访问后首次打开会慢几秒（拉起实例），之后正常，属正常。
- 本程序只读取文件、不写盘，完全符合 SCF「仅 `/tmp` 可写」的限制，无需额外处理。

---

## 注意
- Web 函数要求服务监听 **9000 端口**，`scf_bootstrap` 已 `export PORT=9000`，无需改动 `app.py`。
- 若你在第一步选了 **Python 3.10**，请把 `scf_bootstrap` 第 6 行改为：
  ```bash
  /var/lang/python310/bin/python3.10 app.py
  ```
- 本地仍可用：`cd fund-analyzer && python3 app.py` → http://localhost:8000 。

---

## 备选：腾讯云轻量应用服务器（最像本机、常驻）
如果你更想要「和我 Mac 一模一样、一直开着」的体验，可以买一台最便宜的**轻量应用服务器**（约 ¥60/年）：
1. 镜像选「宝塔/Linux 纯净版」或任意 Linux。
2. 把 `fund-analyzer` 文件夹传上去（scp / 宝塔上传）。
3. 运行：`cd fund-analyzer && nohup python3 app.py > run.log 2>&1 &`
4. 防火墙 / 安全组放通 **8000** 端口。
5. 浏览器访问 `http://<公网IP>:8000` 即可。
比 SCF 省心（无冷启动、无打包），但需要支付服务器费用。

---

## 附：`scf_bootstrap` 内容（丢失权限时照抄）
```bash
#!/bin/bash
# 腾讯云 SCF Web 函数启动文件（必须叫 scf_bootstrap，且需可执行权限 755）
# Web 函数要求 Web Server 监听 9000 端口
cd /var/user
export PORT=9000
/var/lang/python39/bin/python3.9 app.py
```

---

## 方案 B：一键部署脚本（不想在控制台点来点去就选这个）
项目里已附 `deploy.py`，填好密钥后**一条命令**自动建函数 + 传代码 + 建 API 网关触发，把网址打印出来，完全绕过上面所有控制台点击。

前置（需你自己做，安全原因我无法代劳）：
1. 腾讯云控制台 → 访问管理 → [API 密钥管理](https://console.cloud.tencent.com/cam/capi) 生成 `SecretId` / `SecretKey`。
2. 在本目录建 `.env`：
   ```
   TENCENT_SECRET_ID=你的SecretId
   TENCENT_SECRET_KEY=你的SecretKey
   ```
3. 本机安装 SDK： `pip install tencentcloud-sdk-python`

运行：
```bash
python3 deploy.py
```
脚本会创建 `fund-analyzer` 函数（Python3.9 / Web 函数）、上传 `fund-analyzer.zip`、创建「函数 URL」触发（匿名公网），并提示访问网址。
若函数 URL 自动创建失败，脚本会告诉你去「函数详情 → 左侧「函数 URL」→ 新建函数 URL → 授权类型选开放、勾公网 → 确定」点一下即可。
